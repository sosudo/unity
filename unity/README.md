# Unity

Multi-agent autoformalization for Lean 4. Runs natively inside a Lean project;
all artifacts live in a gitignored `.unity/` directory.

Status: rewrite in progress. Commands are scaffolded; pipelines are being ported
from `unity_agent/`.
